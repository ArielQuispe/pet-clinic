name: CI

on: 
  push:
    branches: [ "main"]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs: 
  build:
    runs-on: ubuntu-latest
    steps:
     - uses: actions/checkout@v3
     - name: Run a multi-line script
       run: |
         echo "hola mundo"
  test: 
    runs-on: ubuntu-letest
    steps:
      - name: test
        run: |
          echo "hola 2"
